# Worker-only deps. EchoMimicV3's own requirements.txt is installed separately in the
# Dockerfile and owns torch/diffusers/transformers/xformers.
#
# Audio duration is probed with ffprobe (already in the image) rather than pulling in
# soundfile/librosa for one number, so the worker itself needs only two packages.
runpod>=1.7.0
requests>=2.32.0

# ── Upstream gaps ────────────────────────────────────────────────────────────────────────
# `infer_flash.py` imports these but antgroup/echomimic_v3's requirements.txt does not list
# them, so the image builds clean and then dies at import time on the first job. Found by
# running the real inference on a Pod before building. Add to this list if another surfaces.
pyloudnorm>=0.1.1
